    config = database and app configuration

    controllers = handles app logic and responses

    middleware = controls request processing/auth

    models = defines database schemas

    routes = defines API endpoints

    server.js = main server entry point

    git init
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/MintHerbs/offrian.git
git push -u origin main